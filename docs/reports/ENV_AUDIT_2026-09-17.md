# Environment axes audit — living/dead per surface (#2282 D2)

**Date**: 2026-09-17 · **Measured by**: myia-po-2023 (po-2025 core rows from the #2282 comment, cross-checked where reachable) · **Base**: main `464c1290` + the API-floor branch of this PR.

Every row below is a **measurement**, not a suspicion (STOP&REPAIR doctrine, user mandate 2026-09-17). The audit exists because the JVM campaign outage (#2062/#2276/#2277) was a mechanism: degradation invisible at run level, bands disjoint, local pins extinguishing alarms, KNOWN_ISSUES a resolved-only graveyard.

## Axis × surface table

| Axis | Surface | State (2026-09-17) | Measurement | Owner |
|---|---|---|---|---|
| JVM/Tweety | CI runner | **alive** | Maven on runner; provisioning builds a 1.31 set | — |
| JVM/Tweety | repo principal (po-2025) | **alive** | 76 jars 1.31 assembled | — |
| JVM/Tweety | campaign worktree | **DEAD** | 0 jar (libs/ gitignored), no Maven — #2062 rendered 48/48 on a dead JVM | #2276 (closed, diagnostic), #2282 D1 (manifest canary) |
| JVM/Tweety | repo po-2023 (avant ce PR) | **stale-API** | 33 module fat jars 1.28 + 1 fat jar 1.29 — all old bipolar API (`Evidential/Necessity/DeductiveArgumentationFramework`, measured by zip census); the #2278 fallback served 1.29 → JVM booted, then `BipolarArgumentationFramework` (1.31 API, #1959) was unresolvable | #2278 follow-up — **fixed by this PR** (API floor) |
| JVM/Tweety | repo po-2023 (après ce PR) | **alive** | Maven 3.9.9 installed (`MAVEN_HOME`, portable, `maven_executable()` finds it); 1.31 assembled from Maven Central (49 module jars, 46 s); stale sets archived OUT of the classpath to `C:\Tools\tweety-stale-archive` (34 jars preserved — the legacy /builds/ channel is dead, they are NOT re-downloadable) | this PR + machine env |
| LLM OpenAI | all surfaces | **alive** | keys configured | — |
| LLM OpenRouter | all surfaces | **alive (pinned)** | double per-process pin (`OPENROUTER_BASE_URL` + `OPENROUTER_CHAT_MODEL_ID`); honest-resolution repair in flight | #2281 / PR #2283 |
| LLM self-hosted | po-2023 | **ALIVE — 200** | `GET {endpoint}/models` with the current key → **HTTP 200** (2026-09-17). Contradicts the inherited "401 périmée" reading of #2130 — at least from this machine, endpoint + key are valid today. The axis is nonetheless **never exercised** by any real run | #2130 |
| torch / neural | CI runner | **DEAD (excluded)** | deterministic (runner image cannot load torch) | #1651 |
| torch / neural | po-2023 local | **alive** | `import torch` → 2.2.2 | — |
| Java | po-2023 | **pinned-valid** | system PATH `java` = 1.8.0_503 (below required 11+); `.env` `JAVA_HOME` → `libs/portable_jdk/jdk-17.0.11+9` (present, valid). The pin is the workaround — loud only if one reads `.env`; root `portable_jdk/jdk-17.0.12+7` is a second, apparently orphaned JDK copy | machine env (dedup candidate) |
| `requires_api` band | CI | **NEVER RUN** | `ci.yml:258` runs `-m "not slow and not requires_api"`; the band = **90 tests / 41 files**, and collection itself errors on **6 files** (rot has started) | #1603 DoD 4 + proposal below |

## #2277 DoD 4 — re-render verdict (post-#2278, JVM vivante)

Same input shape as the ×40 WARN motif, **real** chain this time: `initialize_jvm()` **without any pin** → True (1.31 served, Maven-resolved) → real `BipolarHandler.analyze_bipolar_framework` → real extensions `[["a1","a2"]]` (grounded, model_size 2) → ledger `{"status": "evaluated", "degraded": false, "extension_count": 1}` → `_collect_absent_dimensions` → **[]** → gate verdict: **PASS, zero reasons** (with full-length act prose; a deliberately stub-short act1 prose yields an honest WARN for THAT real reason — the support dimension never appears).

The #1913 gate expectation measured correct end-to-end: producer absent → honest WARN; producer present → PASS. Cited on #2277.

## `requires_api` band — proposal for arbitration (#1603 DoD 4)

Measured facts: 90 tests / 41 files, never selected in CI (`ci.yml:258`), 6 collection errors already. Options:

- **A (recommended)**: scheduled weekly workflow running the **light subset first** (`-m "requires_api and llm_light"`), with `OPENAI_API_KEY` secret, junit artifact, fail-loud. Full band monthly or pre-release. Cost envelope: 90 tests × ~$0.01–0.05 ≈ **$0.9–4.5 per full run**; the light subset is a fraction of that. Creating the workflow is user-gated (`.github/workflows/`) — hence a proposal here, not an implementation.
- **A0 (prerequisite, not gated)**: fix the 6 collection errors first — an unrunnable band tests nothing.
- **B**: pre-campaign canary convention — before any real-LLM campaign, run the light subset locally/runner-side (no workflow change; runbook line).
- **C**: dated decision not to run the band — accepted cost: 90 tests permanently unexecuted, rot compounding (already 6 broken).

Recommendation: **A0 + A(light weekly) + B**, decision dated by coordinator/user on the full band.

## Anti-pendules held

- Per-phase degradation untouched (surfaced by #2282 D1's manifest, not forbidden).
- The stale 1.28/1.29 sets were **archived with preservation proof**, not deleted (dead legacy channel ⇒ not re-downloadable).
- torch-in-CI (#1651) documented as the measured condition it is — not "repaired".
